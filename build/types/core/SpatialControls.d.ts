import { Quaternion, Vector3 } from "three";
import { Disposable } from "./Disposable";
import { Settings } from "../settings/Settings";
export declare class SpatialControls implements Disposable, EventListenerObject {
    private domElement;
    private position;
    private quaternion;
    private target;
    settings: Settings;
    private rotationManager;
    private translationManager;
    private strategies;
    private lastScreenPosition;
    private dragging;
    private enabled;
    constructor(position?: Vector3, quaternion?: Quaternion, domElement?: HTMLElement);
    getDomElement(): HTMLElement;
    setDomElement(domElement: HTMLElement): SpatialControls;
    getPosition(): Vector3;
    setPosition(position: Vector3): SpatialControls;
    getQuaternion(): Quaternion;
    setQuaternion(quaternion: Quaternion): SpatialControls;
    getTarget(target: Vector3): Vector3;
    setTarget(target: Vector3): SpatialControls;
    getViewDirection(view: Vector3): Vector3;
    setOrbitEnabled(orbit: boolean): SpatialControls;
    copy(controls: SpatialControls): SpatialControls;
    clone(): SpatialControls;
    private handlePointerMoveEvent;
    private handleTouchMoveEvent;
    private handleMainPointerButton;
    private handleAuxiliaryPointerButton;
    private handleSecondaryPointerButton;
    private handlePointerButtonEvent;
    private handleTouchEvent;
    private handleKeyboardEvent;
    private handleWheelEvent;
    private handlePointerLockEvent;
    handleEvent(event: Event): void;
    update(deltaTime: number): void;
    moveTo(position: Vector3): SpatialControls;
    lookAt(point: Vector3): SpatialControls;
    private setPointerLocked;
    setEnabled(enabled?: boolean): SpatialControls;
    dispose(): void;
}
